{
  "name": "saju",
  "private": true,
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev -- --port 3001",
    "dev:backend": "cd backend && PYTHONPATH=$PWD /Users/psg/saju/.venv/bin/python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "dev": "node scripts/dev.mjs",
    "dev:clean": "node scripts/clean.mjs --ports 3001,8000,3000",
    "dev:reset": "npm run dev:clean && npm run stop",
    "start:frontend": "cd frontend && npm run dev -- --port 3001",
    "start:backend": "cd backend && PYTHONPATH=$PWD /Users/psg/saju/.venv/bin/python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "stop": "node scripts/stop.mjs",
    "build": "cd frontend && npm run build"
  }
}
